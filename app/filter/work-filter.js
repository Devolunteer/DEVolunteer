'use strict';

module.exports = function(){
  return function(developers, type) {
    let workSort = filterByWorkType(type);


  }
}






// devListCtrl.dev is what we're dealing with.  all the filters are arrays.  




//1. filter by type of work (website, mobile app, web app)
// 2. filter by language (js, html/css, python, angular, react, don't know)
//3. show only devs who are available now.
//4. filter by reviews
